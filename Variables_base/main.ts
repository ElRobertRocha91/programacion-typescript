// Variable de tipo string (cadena)
var nombre: string = "Admin";

// Variable de tipo number (número)
var edad: number = 39;

// Variable de tipo booleano (verdadero o falso)
var esDesarrolladorWeb: boolean = true;

// Variables que puede ser undefined o string
var mes: string | undefined;

// Variable que inicializa como null
var variableNull: null = null;

console.log("Mes inicial:", mes);

mes = "Enero";
console.log("Mes actualizado:", mes);

console.log(`Hola ${nombre}`);